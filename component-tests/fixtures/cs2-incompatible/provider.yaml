# Deliberately missing /pets entirely (only /other exists) -> checkPresence finds
# the consumer-configured operation absent from the provider -> verdict=incompatible.
openapi: 3.0.3
info:
  title: provider-fixture-incompatible
  version: "1.0.0"
paths:
  /other:
    get:
      responses:
        "200":
          description: OK
