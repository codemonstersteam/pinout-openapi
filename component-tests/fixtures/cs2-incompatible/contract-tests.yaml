# CS2 fixture (verdict guard): consumer and provider both load, but the provider is
# missing the configured /pets get operation entirely -> checkPresence finding ->
# verdict=incompatible (exit 1), NOT an I/O-adapter failure.
consumer:
  spec_path: "/fixtures/cs2-incompatible/consumer.yaml"
  name: consumer-fixture
  operations:
    - path: /pets
      method: get
provider:
  spec_path: "/fixtures/cs2-incompatible/provider.yaml"
  name: provider-fixture
