# CS7 fixture: provider-stub answers /timeout by sleeping 5s before replying;
# settings.timeout=1 bounds the fetch below that -> ProviderSpecReader.Load's
# fetch exceeds the deadline -> PROVIDER_TIMEOUT (exit 3).
consumer:
  spec_path: "/fixtures/cs7-provider-timeout/consumer.yaml"
  name: consumer-fixture
  operations:
    - path: /pets
      method: get
provider:
  spec_url: "http://provider-stub:8080/timeout"
  name: provider-fixture
settings:
  timeout: 1
