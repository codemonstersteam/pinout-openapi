# CS4 fixture: consumer.spec_path points nowhere (no such file on disk) ->
# ConsumerSpecReader.Load fails to read -> SPEC_UNREADABLE (exit 2).
consumer:
  spec_path: "/fixtures/cs4-consumer-unreadable/does-not-exist.yaml"
  name: consumer-fixture
  operations:
    - path: /pets
      method: get
provider:
  spec_path: "/fixtures/cs4-consumer-unreadable/provider.yaml"
  name: provider-fixture
