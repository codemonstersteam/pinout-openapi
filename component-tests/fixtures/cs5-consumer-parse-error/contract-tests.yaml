# CS5 fixture: consumer.spec_path resolves to a file that reads fine but is not a
# valid OpenAPI 3.x document -> ConsumerSpecReader.Load's kin-openapi step fails to
# parse -> SPEC_PARSE_ERROR (exit 2).
consumer:
  spec_path: "/fixtures/cs5-consumer-parse-error/consumer-malformed.yaml"
  name: consumer-fixture
  operations:
    - path: /pets
      method: get
provider:
  spec_path: "/fixtures/cs5-consumer-parse-error/provider.yaml"
  name: provider-fixture
