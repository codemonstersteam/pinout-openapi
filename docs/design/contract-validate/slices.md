# slices — contract-validate

Один внешний вход = один срез.

| Внешний вход | ID | Срез | Ингресс |
|---|---|---|---|
| CLI `validate <config>` | 01 | validate-contract | cobra-команда `validate` |

MVP — один срез. `version`/`info` тривиальны (без алгоритма), добавляются вне пакета проектирования при необходимости.
